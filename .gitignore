.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.eslintignore
.prettierignore
/.cache/
*.tsbuildinfo

# Built by `npm run build-resume`. Not committed — the deploy builds them,
# so they cannot go stale.
/output/
/.resume-build/
/static/Chun-Yu-Echo-Shih-Software-Engineer.pdf

# Local agent tooling: installed per machine, not part of this project.
/_bmad/
/.agents/
# The whole directory, not just settings and skills: it also accumulates
# scratch git worktrees, which are full checkouts of this repo. Twenty-odd
# megabytes of it sat untracked, one `git add -A` away from being committed.
# Negate a specific path here if something under it is ever worth sharing.
.claude/

# visual brainstorm companion
.superpowers/

# Working documents: specs, audits, notes. They are written for me, not for
# readers of a public repo — that is how the portfolio audit leaked.
/docs/

# Playwright output: reports and traces are per-run, not source.
/playwright-report/
/test-results/
