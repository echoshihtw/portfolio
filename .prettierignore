.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock
/.cache/

# Generated, never hand-edited — see resume-invariants. The résumé build emits
# it unformatted, so checking it here would fail on every rebuild.
src/lib/resumeData.ts

# Same reason: `npm run build-icons` emits this and prettier would reformat
# it, so a regenerate would fail the check on a file nobody hand-edits.
src/lib/generated/

# Installed tooling and build outputs, not project source.
_bmad
output
static/resume.pdf
